{
  "name": "tokenmaxxer",
  "version": "1.0.0",
  "description": "Adversarial Tokenmaxxing — same-length text variants that use more BPE tokens.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "keywords": ["tiktoken", "bpe", "tokenizer", "tokenmaxxing"],
  "license": "ISC",
  "dependencies": {
    "js-tiktoken": "^1.0.21"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
